<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.habitatapi.habit" >

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
 To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" /> <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginPage"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".RegisterPageOneActivity"
            android:label="@string/title_activity_register_page_one"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".RegisterPageTwoActivity"
            android:label="@string/title_activity_register_page_two"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".RegisterPageThreeActivity"
            android:label="@string/title_activity_register_page_three"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".RegisterPageFourActivity"
            android:label="@string/title_activity_register_page_four"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".RegisterPageFiveActivity"
            android:label="@string/title_activity_register_page_five"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard" >
        </activity>
    </application>

</manifest>
